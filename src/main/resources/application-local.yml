spring:
  datasource:
    url: jdbc:mysql://localhost:3307/jupjup-ticket-server
    username: root
    password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: update

jupjup-api:
  url: http://localhost:8080